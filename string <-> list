filename = "rosalind_seq.txt"

#turn Rosalind data into a list of integers
with open(filename) as file_object:
    Ros_seq = file_object.read().strip("\n").split(" ")
for i in Ros_seq:
    i = int(i)

#for output, turn the list of integers back into a string
formatted = " ".join(map(str, Ros_seq))
print(formatter)
